name: ai_guide_api
description: A Dart API Client for the AI_Guide API.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^1.1.0
  json_annotation: ^4.6.0

dev_dependencies:
  build_runner: ^2.4.6
  json_serializable: ^6.3.1
  mocktail: ^1.0.0
  test: ^1.16.4
